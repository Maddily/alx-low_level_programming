#include <stdio.h>

/**
  * main - Prints the numbers from 0 to 9
  * Return: Returns 0
  */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
