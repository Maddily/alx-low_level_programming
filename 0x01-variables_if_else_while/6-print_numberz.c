#include <stdio.h>

/**
  * main - Prints the numbers from 0 to 9
  * Return: Returns 0
  */
int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar((char)(n));
		n++;
	}
	putchar('\n');
	return (0);
}
